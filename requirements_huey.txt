# Requirements for Huey (task queue) integration.
# Kept as a separate requirements file for backward compatibility with install/CI scripts
# which run `pip install -r requirements_huey.txt`.
# Huey-related dependencies are currently listed directly in requirements.txt.
 

